import type { SDKMessages } from '../types';
import { en } from './en';

// TODO: Translate all strings from English
// For now, falls back to English defaults
export const zh: SDKMessages = { ...en };
